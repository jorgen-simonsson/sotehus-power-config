# Allow anonymous connections? (no = require username/password)
allow_anonymous false

# Password file
password_file /mosquitto/config/passwords.txt

# Listener on default MQTT port
listener 1883

# Optional: WebSocket listener
listener 9001
protocol websockets

# Logging
log_dest stdout
